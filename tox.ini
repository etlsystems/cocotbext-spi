[tox]
isolated_build = true
envlist = py{39,310,311,312},lint,check-dist

[gh-actions]
python =
    3.9: py39
    3.10: py310
    3.11: py311
    3.12: py312

[flake8]
max-line-length = 120

[testenv]
deps = uv
commands =
    uv sync --active
    uv run --active pytest tests -n auto

[testenv:lint]
deps = uv
commands =
    uv sync --active --only-group lint
    uv run --active flake8 cocotbext/ tests/

[testenv:check-dist]
skip_install = true
deps =
    uv
    twine
commands =
    uv build
    twine check dist/*
